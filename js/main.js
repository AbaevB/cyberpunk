const swiper = new Swiper('.swiper-container', {
  // Optional parameters
  loop: true,

  autoplay: {
    delay: 5000,
  },
  effect: 'flip',
  effect: 'fade',
  fadeEffect: {
    crossFade: true
  },
});
